[pytest]
minversion = 6.0
addopts = -q
testpaths = tests

# Note: pytest adds the project root to sys.path. To import 'app' during tests without installing,
# run pytest from the repository root. Alternatively, install the package in editable mode:
# pip install -e .
